<body>
    <div class="card">
        <img src="" alt="profile picture" style="width:100%">
        <h1>{{name}}</h1>
        <p>{{breed}}</p>
        <p>{{activity_level}}</p>
        <p>{{email}}</p>
        <p>{{address, city, state, zip}}</p>
      </div>
    </div>
</body>